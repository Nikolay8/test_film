<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:clickable="true"
    android:fillViewport="true"
    android:focusable="true"
    android:scrollbars="none">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@android:color/black"
        android:orientation="vertical"
        android:paddingStart="10dp"
        android:paddingTop="16dp"
        android:paddingEnd="10dp"
        android:paddingBottom="16dp">

        <TextView
            android:id="@+id/detail_fragment_original_title_text_view"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:fontFamily="sans-serif-medium"
            android:padding="10dp"
            android:textSize="20sp"
            tools:text="Title" />

        <TextView
            android:id="@+id/detail_fragment_overview_text_view"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:fontFamily="sans-serif"
            android:padding="10dp"
            android:textSize="16sp"
            tools:text="overview" />

        <TextView
            android:id="@+id/detail_fragment_release_date_text_view"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:fontFamily="sans-serif"
            android:padding="10dp"
            android:textSize="16sp"
            tools:text="release date" />


        <TextView
            android:id="@+id/detail_fragment_revenue_text_view"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:fontFamily="sans-serif"
            android:padding="10dp"
            android:textSize="16sp"
            tools:text="revenue" />

        <ImageView
            android:id="@+id/detail_fragment_poster_image_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:contentDescription="@string/downloading_image"
            android:scaleType="fitCenter" />


    </LinearLayout>


</ScrollView>